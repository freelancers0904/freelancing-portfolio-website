# Pricing — drop redundant first-line in Grow & Dominate

## Change (only in `src/components/Pricing.tsx`)

- **Grow plan**: remove `'Everything in Launch'` from features
- **Dominate plan**: remove `'Everything in Grow'` from features

Each plan now reads cleanly on its own — perfect for reusing in client quotes later. No other edits.

## My take
Agreed, this is the cleanest option. Each tier already lists strong, specific deliverables, so the "Everything in X" line was just filler. Removing it tightens the cards visually too.
